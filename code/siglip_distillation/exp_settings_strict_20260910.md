# 严格排除验证与测试数据后的重新蒸馏

四个尺寸全部随机初始化，从第1轮训练至300轮，不加载任何旧学生权重或优化器。旧学生目录已改名作废。教师仍是固定的SigLIP2；只复用通过新划分检查的逐样本教师表示，教师不在本项目训练。

数据清单：`data/siglip_distillation_strict_20260910/`；固定训练、验证和测试划分：`data/router_splits_strict_20260910/`。CUB按类别从原训练集固定留出20%验证，种子42；工地排除路由验证701张；gRefCOCO按COCO图片编号排除非训练图片；NLVR2排除验证、测试来源网址。验证/测试文本的规范化重复项也不进入蒸馏或词表训练。具体数量及检查范围以`split_audit.json`为准，不宣称检查了外部教师预训练集或任意未知近重复图片。

文字从允许的训练样本重建，并重新训练4096词词表，最大长度64。图像224×224、16×16图块；四档学生各两层Transformer，维度及总参数与上次相同。

仍为图像与文本两个教师表示的联合蒸馏，两个余弦损失权重均1，不加入新的关系损失。两路独立采样。每轮以较多的一路完整遍历计，另一端循环补齐。

四张A800每卡负责一个尺寸，共用父进程保有的0号卡图文缓存；每个尺寸批量2048，AdamW，学习率0.0001，权重衰减0.01，随机种子42，总300轮，逐步余弦衰减。正式启动前使用同配置做3步接线检查；正式训练重新随机初始化，不继承检查权重。

每轮保存可恢复权重、逐步及逐轮日志，每25轮及最后一轮保存独立权重。每个模型的`training_started.json`记录真正进入训练的时间；正式运行5分钟后的预计工期依据这次实测，不沿用上次混合硬件耗时。
