Locate all and only the visible objects that satisfy the entire referring expression below. Every stated object type, appearance, position, and spatial relation matters. Do not return the closest-looking object when part of the expression is missing or contradicted. Some expressions intentionally have no matching object, so an empty list is a normal answer whenever the full expression is not visibly satisfied.

The expression may identify zero, one, or multiple objects. When separate target descriptions are joined by words such as "and", return a separate tight box for every described target rather than one box around the group. When "and" only joins properties of one object, all those properties must hold for that one box. Do not merge neighboring objects into one box.

Coordinates must be xyxy numbers normalized from 0 to 1000, and each box must have a confidence score from 0 to 1. Reply with exactly one complete JSON object and no Markdown or explanation, using this form: {"objects":[{"bbox_2d":[120,85,640,910],"score":0.92}]}. An answer with no match is exactly {"objects":[]}.

Referring expression: {expression}

